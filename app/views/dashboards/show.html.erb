<h1>Dashboard</h1>

<% if @eventbrite_auth_url %>
  <%= link_to "Connect to Eventbrite", @eventbrite_auth_url %>
<% end %>

<ul class="events">
  <% @events.each do |event| %>
    <li data-id="<%= event.id %>">
      <%= event.name %>
    </li>
  <% end %>
</ul>
